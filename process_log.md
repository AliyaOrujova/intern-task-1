# Process log
## 25th of June 2026 
Today was the first day of the assignment, and I started it off by reading it carefully and choosing the option that I want to implement. I decided to try to finish the code a few days before the deadline if I am able to, so that I can work on the report, and also so that I have time to go through the code a final time and get ready to present it. I learned more about merge sort and insertion sort, and implemented their algorithms in the sorting_algorithms.py file. I chose some file names in advance so that I can clearly remember all of the components of the task and to be able to view them separately. I also initialised the git repository and created a new project on my Desktop, renamed the branch to main, and then started writing the predictions for some of the types of data. However, when I was writing the functions for generating the testing data types, I decided to add a few more which I did not add into the predictions. I will update the predictions as soon as possible to include that. 

I implemented insertion sort in the sorting_algorithms.py, and at the beginning I was using a hardcoded list, but I changed it into a function, as it will need to be called by other files, like test_algorithms.py annd benchmark_sorting.py. I did the same thing for merge sort, for which I had three functions, the main function for merge, a recursive helper function and a merge function, and I added comments to the code to help explain all of the parts. After that, I worked on writing the functions which generate test data, and so far I have random data, sorted data, reverse-sorted data, duplicate-heavy data, nearly sorted data, all-equal data, and few-unique-values data. I realised that for the duplicate-heavy data, for small numbers of n it wouldn't work so I decided to change it to be relative to n, and I fixed the issue of n=0 in the nearly sorted function, as the hcode would choose a random index from an empty list, which is a problem so I just returned the data when the list was too small. Then I pushed the main branch to github, because I realised that I had forgotten to connect the local repository to GitHub. 

## 26th of June 2026 
Today I worked on the benchmark_sorting file, and it was to basically create the data that will be used for plotting. And I also worked on plotting said data with matplotlib and pandas. I would say that both parts were challenging for me. At first I wanted to use a list of n but then I decided to use sizes 0 to 500 with step 1, so every integer value, since n is an integer. I wanted to do that so that I would get accurate results and also because I will be plotting it on graphs, so that made sense to me. I think I also worked on the tests for the sorting algorithms yesterday evening after I wrote yesterday's log to make sure that they were working correctly, and all tests passed. I used an array of small manual tests and also I generated bigger tests and tried it on all the data types just in case. For the benchmarking code, I used time.per_counter() and I also made sure to include a warm-up function. I made sure to fix a seed for the random function so that the inputs would be reproducible. In terms of pandas, I used it for reading the csv and matplotlib for generating the graphs. In VS Code, I had an issue with matplotlib.pyplot since VS Code was using a different python environment from the one in which I installed the packages.I resolved this issue in the end by making sure that I was using the righ environment. I also adjusted the x-axis of the graphs. By default it only showed the ticks for every 100, but I made it every 25. I also realised that the program was showing the crossover to be at n=0 for everything and I fixed it by ignoring crossover points less than 2. I also made sure that the measurements were not as affected by random timing noise by making sure that merge sort was faster 5 times in a row before it was counted as a crossover. First, I separated the data into insertion sort and merge sort, and then combined it so that for every n, the time for the merge sort and for the insertion sort were on the same row, and then sorted by input size, and then I used iloc to search for windows of 5 sizes at a time. current_rows = combined.iloc[i:i + consecutive_required] was the line that I used, and in here, combined is the DataFrame after all the results for the insertion and merge are on the same row for each n, and iloc selects rows by their integer position, and i:i + consecutive_required means that we start at row i and then stop before row i + consecutive_required. And then, merge_is_faster_every_time = all(
    current_rows["average_time_seconds_merge"]
    < current_rows["average_time_seconds_insertion"]
)
is used to check if merge sort is faster in every row in that group.