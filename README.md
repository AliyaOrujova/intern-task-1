How to Run the Code

First, install the required libraries:

pip install pandas matplotlib

Run the tests to check that the sorting algorithms work correctly:

python test_algorithms.py

Run the benchmark:

python benchmark_sorting.py

Generate the graphs, crossover summary, and PDF report:

python plot_results.py

After running the scripts, the output files will be saved in the results/ folder.
